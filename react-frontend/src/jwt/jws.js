import React from "react";

export default class Jws extends React.Component
{

    constructor(props)
    {
        super(props);
    }

    render()
    {
        return (
            this.props.children
        )
    }
}

